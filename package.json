{
  "name": "br-lawn-care-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-pages.mjs",
    "dev": "node serve.mjs",
    "audit": "node scripts/audit-site.mjs",
    "test": "node scripts/test-site.mjs",
    "verify": "npm run build && npm run audit && npm test"
  }
}
